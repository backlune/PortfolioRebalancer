<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="PortfolioRebalancer.App.Views.ImportDialog"
        Title="ImportDialog">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="0.7*" />
      <RowDefinition Height="0.2*" />
      <RowDefinition Height="0.1*" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="0.7*" />
      <ColumnDefinition Width="0.3*" />
    </Grid.ColumnDefinitions>

    <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
      <TextBlock  Text="Portfolio Id" />
      <TextBox Text="{Binding PortfolioId, Mode=TwoWay}" />
    </StackPanel>

    <Button Grid.Row="0" Grid.Column="1" Content="Start Import" Command="{Binding StartImport, Mode=TwoWay}" />

    <Button x:Name="Close" Content="Close" Grid.Row="1" Grid.Column="1" />
  </Grid>

</Window>
